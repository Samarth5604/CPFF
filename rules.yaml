# ======================================================
# CPFF Firewall Rules (Balanced & Realistic Demo Set)
# ======================================================

# 🚫 1. Block unencrypted HTTP
- id: 1
  action: block
  protocol: TCP
  dst_port: 80
  comment: Block unencrypted HTTP web traffic (insecure websites)

# 🚫 4. Block selected GeoIP region (for demo)
- id: 4
  action: block
  geoip_country: RU
  comment: Block traffic from Russia (GeoIP demonstration)

# 🚫 4. Block selected GeoIP region (for demo)
- id: 4
  action: block
  geoip_country: US
  comment: Block traffic from Russia (GeoIP demonstration)

# 🚫 4. Block selected GeoIP region (for demo)
- id: 4
  action: block
  geoip_country: PK
  comment: Block traffic from Russia (GeoIP demonstration)

# 🚫 4. Block selected GeoIP region (for demo)
- id: 4
  action: block
  geoip_country: NK
  comment: Block traffic from Russia (GeoIP demonstration)

# ✅ 6. Allow local LAN traffic
- id: 6
  action: allow
  src_ip: 192.168.0.0/16
  dst_ip: 192.168.0.0/16
  comment: Allow internal LAN communications

# ✅ 7. Allow DNS lookups
- id: 7
  action: allow
  protocol: UDP
  dst_port: 53
  comment: Allow DNS queries

# ✅ 8. Allow HTTPS
- id: 8
  action: allow
  protocol: TCP
  dst_port: 443
  comment: Allow secure HTTPS traffic

# ✅ 9. Default catch-all (allow everything else)
- id: 9
  action: allow